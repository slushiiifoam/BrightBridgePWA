# Public project URL used only by the server-side journal function.
SUPABASE_URL=https://pyqznelkiujkmviedlha.supabase.co

# Add this secret in Netlify Site configuration > Environment variables.
# Never prefix it with VITE_ and never commit its real value.
SUPABASE_SERVICE_ROLE_KEY=replace_with_your_server_only_service_role_key
